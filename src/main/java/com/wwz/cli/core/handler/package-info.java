/**
 * Handler contracts and support classes.
 *
 * <p>Handlers are where application command behavior starts. The support base class lets
 * applications register several related command operations in one handler, while the
 * system handler support provides common help/clear/unknown behavior.</p>
 */
package com.wwz.cli.core.handler;
