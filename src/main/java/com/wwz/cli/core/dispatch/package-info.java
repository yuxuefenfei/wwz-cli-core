/**
 * Command resolution and dispatching.
 *
 * <p>This package turns parsed command names into typed commands and routes them to the
 * handler that owns each command. It is intentionally free of business rules.</p>
 */
package com.wwz.cli.core.dispatch;
