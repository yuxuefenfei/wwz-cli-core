/**
 * Input receiving boundary.
 *
 * <p>A receiver is the first application-facing step after the shell reads a line.
 * The default receiver delegates to a parser, while custom receivers can add auditing,
 * preprocessing, or command normalization.</p>
 */
package com.wwz.cli.core.receiver;
